(ns wombats.game.processor)

(defn process-user-decisions
  "Process users decisions by running their code through AWS Lambda"
  [game-state]
  ;; TODO #162
  game-state)

(defn process-turn-resolution
  [game-state]
  ;; TODO #151
  game-state)
